services:
  - type: web
    name: trading-alert-webhook
    env: node
    buildCommand: npm install
    startCommand: node webhook-server.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false